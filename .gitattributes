# Force LF line endings for files that run under bash (WSL / container)
*.sh text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.tmpl text eol=lf
*.conf text eol=lf
.env text eol=lf
*.env text eol=lf

# Everything else: normalize to LF on commit, let the OS decide on checkout
* text=auto
